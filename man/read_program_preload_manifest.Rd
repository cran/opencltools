% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_program_preload.R
\name{read_program_preload_manifest}
\alias{read_program_preload_manifest}
\title{Read a program preload manifest}
\usage{
read_program_preload_manifest(
  manifest_relative_path = "program_preload_manifest.tsv",
  source_package = "nmathopencl",
  manifest_path = NULL
)
}
\arguments{
\item{manifest_relative_path}{Path relative to \verb{inst/cl/} inside
\code{source_package}. Defaults to \code{"program_preload_manifest.tsv"}.}

\item{source_package}{Installed package that owns the manifest and prelude
sources. Defaults to \code{"nmathopencl"}.}

\item{manifest_path}{Optional absolute path to a \code{.tsv} manifest. When
supplied, \code{source_package} is still recorded in metadata but
\code{system.file()} is not used.}
}
\value{
A \code{data.frame} with columns \code{rank}, \code{kind}
(\code{"file"} or \code{"library"}), and \code{rel_path}, sorted by
\code{rank}.
}
\description{
Reads the tab-separated manifest that lists the fixed OpenCL prelude
(single files and annotated library directories) in load order.  Production
manifests ship with downstream packages such as \strong{nmathopencl}; this package
includes a small teaching manifest at
\code{inst/cl/program_preload_manifest.tsv} for examples and tests.
}
\details{
When a companion \code{program_preload_manifest.rds} exists beside the
\code{.tsv} file (see \link{write_program_preload_manifest}), the RDS copy
is preferred for repeated R-side reads.
}
\examples{
############################ Start of load_program_preload example ########################

manifest <- read_program_preload_manifest(source_package = "opencltools")
print(manifest)

preload <- load_program_preload(source_package = "opencltools")
cat("Preload bytes:", attr(preload, "nbytes_concatenated"), "\n")

## End of load_program_preload example
}
\seealso{
\link{load_program_preload}, \link{write_program_preload_manifest}

Other OpenCL program preload: 
\code{\link{load_program_preload}()},
\code{\link{write_program_preload_manifest}()}
}
\concept{OpenCL program preload}
